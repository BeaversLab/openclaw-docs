---
summary: "Déployer OpenClaw sur Railway avec un modèle en un clic"
read_when:
  - Deploying OpenClaw to Railway
  - You want a one-click cloud deploy with browser-based Control UI
title: "Railway"
---

Déployez OpenClaw sur Railway avec un modèle en un clic et accédez-y via l'interface web de contrôle.
C'est le chemin le plus simple « sans terminal sur le serveur » : Railway exécute le Gateway pour vous.

## Liste de contrôle rapide (nouveaux utilisateurs)

1. Cliquez sur **Déployer sur Railway** (ci-dessous).
2. Ajoutez un **Volume** monté sur `/data`.
3. Définissez les **Variables** requises (au moins `OPENCLAW_GATEWAY_PORT` et `OPENCLAW_GATEWAY_TOKEN`).
4. Activez le **Proxy HTTP** sur le port `8080`.
5. Ouvrez `https://<your-railway-domain>/openclaw` et connectez-vous en utilisant le secret partagé configuré. Ce modèle utilise `OPENCLAW_GATEWAY_TOKEN` par défaut ; si vous le remplacez par une authentification par mot de passe, utilisez plutôt ce mot de passe.

## Déploiement en un clic

<a href="https://railway.com/deploy/clawdbot-railway-template" target="_blank" rel="noreferrer">
  Déployer sur Railway
</a>

Après le déploiement, trouvez votre URL publique dans **Railway → votre service → Paramètres → Domaines**.

Railway effectuera soit :

- vous fournir un domaine généré (souvent `https://<something>.up.railway.app`), ou
- utiliser votre domaine personnalisé si vous en avez attaché un.

Ensuite, ouvrez :

- `https://<your-railway-domain>/openclaw` — Interface de contrôle

## Ce que vous obtenez

- Gateway OpenClaw hébergé + Interface de contrôle
- Stockage persistant via Railway Volume (`/data`) afin que `openclaw.json`,
  les `auth-profiles.json` par agent, l'état des channel/provider, les sessions et
  l'espace de travail survivent aux redéploiements

## Paramètres Railway requis

### Mise en réseau publique

Activez le **proxy HTTP** pour le service.

- Port : `8080`

### Volume (requis)

Attachez un volume monté à :

- `/data`

### Variables

Définissez ces variables sur le service :

- `OPENCLAW_GATEWAY_PORT=8080` (requis — doit correspondre au port dans Public Networking)
- `OPENCLAW_GATEWAY_TOKEN` (requis ; à traiter comme un secret administrateur)
- `OPENCLAW_STATE_DIR=/data/.openclaw` (recommandé)
- `OPENCLAW_WORKSPACE_DIR=/data/workspace` (recommandé)

## Connecter un channel

Utilisez l'interface de contrôle à `/openclaw` ou exécutez `openclaw onboard` via le shell de Railway pour les instructions de configuration du channel :

- [Telegram](/en/channels/telegram) (le plus rapide — juste un jeton de bot)
- [Discord](/en/channels/discord)
- [Tous les channels](/en/channels)

## Sauvegardes et migration

Exportez votre état, votre configuration, vos profils d'authentification et votre espace de travail :

```bash
openclaw backup create
```

Cela crée une archive de sauvegarde portable avec l'état OpenClaw plus tout
espace de travail configuré. Voir [Backup](/en/cli/backup) pour plus de détails.

## Étapes suivantes

- Configurez les channels de messagerie : [Channels](/en/channels)
- Configurez le Gateway : [configuration du Gateway](/en/gateway/configuration)
- Gardez OpenClaw à jour : [Updating](/en/install/updating)
