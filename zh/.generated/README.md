# 生成的文档工件

这些基准工件是根据仓库拥有的 OpenClaw 配置架构以及捆绑的渠道/插件元数据生成的。

- 请勿手动编辑 `config-baseline.json`。
- 请勿手动编辑 `config-baseline.jsonl`。
- 使用 `pnpm config:docs:gen` 重新生成它。
- 在 CI 中或本地使用 `pnpm config:docs:check` 验证它。

import zh from "/components/footer/zh.mdx";

<zh />
